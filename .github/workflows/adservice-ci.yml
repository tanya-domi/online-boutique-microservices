name: AdService CI/CD

on:
  push:
    branches:
      - 'feature/**'
      - main
    paths:
      - 'adservice/**'
  pull_request:
    branches:
      - main
    paths:
      - 'adservice/**'

jobs:
  build:
    uses: ./.github/workflows/ci-reusable.yml
    with:
      service_name: adservice
      dockerfile_path: adservice/Dockerfile
      build_context: adservice
      image_name: ghcr.io/${{ github.repository_owner }}/adservice
      language: java
    secrets: inherit

  deploy:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: build
    uses: ./.github/workflows/cd-reusable.yml
    with:
      service_name: adservice
      image_name: ghcr.io/myorg/adservice
    secrets: inherit
